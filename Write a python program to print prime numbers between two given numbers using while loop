a=int(input('enter first number: '))
b=int(input('enter second number: '))

print('prime numbers are: ')

while(a<=b):
   if a>1:
      is_prime=True
      for i in range(2,a):
         if(a%i==0):
            is_prime=False
            break
      if is_prime:
          print(a)
   a=a+1
